[project]
name = "stat3007-grokking-animations"
version = "0.1.0"
description = "Manim scenes for the G2 grokking presentation and for explaining the maths to the group."
requires-python = ">=3.11"
dependencies = [
    "manim>=0.18",
    "numpy>=1.26",
]

[tool.uv]
package = false
